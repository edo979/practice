import {StyleSheet, Text, View} from 'react-native';

const AddPlace = () => {
  return (
    <View>
      <Text>AddPlace</Text>
    </View>
  );
};

export default AddPlace;
const styles = StyleSheet.create({});
