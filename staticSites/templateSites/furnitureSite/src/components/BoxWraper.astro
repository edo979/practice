---
export interface Props {
  margins?: string
  flexProp?: string
}

const { margins, flexProp = 'justify-between' } = Astro.props
---

<div
  class:list={[
    'container mx-auto flex flex-col gap-10 sm:gap-8 sm:flex-row sm:items-stretch',
    margins,
    flexProp,
  ]}
>
  <slot />
</div>
